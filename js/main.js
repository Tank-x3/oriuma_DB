/**
 * Main Application Logic
 * 共通処理および初期化処理
 */

// ページ読み込み完了時の処理
document.addEventListener('DOMContentLoaded', () => {
    console.log('Oriuma DB: Initialized.');

    // ページ固有の初期化処理があればここで呼び出す
});
